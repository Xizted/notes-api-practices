{
  "name": "todoapp-backend",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "bcrypt": "^5.0.1",
    "cors": "^2.8.5",
    "cross-env": "^7.0.3",
    "dotenv": "^16.0.1",
    "express": "^4.18.1",
    "jsonwebtoken": "^8.5.1",
    "mongoose": "^6.3.6",
    "mongoose-unique-validator": "^3.1.0"
  },
  "scripts": {
    "start": "NODE_ENV=development node ./src/index.js",
    "dev": "NODE_ENV=production nodemon ./src/index.js",
    "jest": "NODE_ENV=test PORT=0 jest --verbose --silent --forceExit",
    "jest:watch": "yarn jest --watchAll"
  },
  "devDependencies": {
    "jest": "^28.1.1",
    "morgan": "^1.10.0",
    "nodemon": "^2.0.16",
    "supertest": "^6.2.3"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
